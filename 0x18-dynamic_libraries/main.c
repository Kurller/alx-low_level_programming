#include <stdio.h>
#include "main.h"

int main()
{
printf("The product of a and b is %d, and the divisionof a and b is %d,the subtraction of a and b is %d, modulus of a and b is %d, addition of a and b is %d\n",mul(10, 20), div(30, 2), sub(5, 2), mod(10, 2), add(4, 7));
return 0;
}
