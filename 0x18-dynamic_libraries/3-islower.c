
#include "main.h"
/**
 * _islower - check if the charater is lowercase
 * @c: a character argument
 * Return: return 0 and 1 depending on the condition
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
